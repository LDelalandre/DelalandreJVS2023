source("scripts/Packages.R")

# Load and process raw data ####
# NB skip this step if you did it once

source("scripts/Data_traits.R") # Load traits per group of traits (e.g. LeafMorpho)


# Load raw and processed data ####